### gal engin -> lib

file(GLOB_RECURSE game_includes src/*.h  )
file(GLOB_RECURSE game_src src/*.cpp )

message("game_includes ${game_includes}")

add_executable(Linmeng ./main.cpp ${game_src} ${game_includes}  )

target_compile_features(Linmeng PUBLIC cxx_std_17)
target_include_directories(Linmeng PUBLIC src pureHead)
target_link_libraries(Linmeng PUBLIC ${3rd_LIBS})

# target_precompile_headers(Linmeng PUBLIC ./pch.h)


message("3rd libs  ${3rd_LIBS}")
message("SDL FOUND ${SDL2_FOUND}")
